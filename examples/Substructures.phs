-- Not tested yet
module Substructures(substructures) where
import Base(para)
import Flatten(fl_rec)

-- substructures :: d a -> [d a]
-- all substructures as a list
substructures = para (\x -> \y -> x : concat (fl_rec y))
