module Main where
import ConstructorName(constructorName,constructors)
data Test a = A a | B (Test a) [Test a] | C (Test a) (Test a) (Test a)

main = print (map constructorName (constructors::[Test a]))

--expected output: ["A", "B", "C"]
